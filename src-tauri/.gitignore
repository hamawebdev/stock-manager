# Generated by Cargo
# will have compiled files and executables
/target/

# Bundled fixed-version WebView2 runtime (Win7 support). The large runtime
# binaries are fetched at build time (CI) into this folder — see its README.
# Keep the folder + README in git, ignore the downloaded runtime payload.
/webview2-runtime/*
!/webview2-runtime/README.md
